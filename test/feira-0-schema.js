var assert = require('chai').assert;

describe( "Feira Schema", function(){
  var schema;
  it("should be required without errors", function(){
    schema = require( '../backend/model/feira/FeiraSchema' );
  })
})
